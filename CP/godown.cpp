#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    vector<int> arr;
    int n;
    while (cin>>n) {
        arr.push_back(n);
    }

    n = arr.size();
    int k = arr[n - 1];
    arr.pop_back();

    
    return 0;
}